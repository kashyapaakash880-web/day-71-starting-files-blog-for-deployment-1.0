# Python 3.14-compatible requirements for Day 71 Flask Blog
#
# Based on the uploaded main.py.
# This keeps the existing application structure while updating packages
# where Python 3.14 compatibility requires it.
#
# IMPORTANT:
# Flask-Gravatar 0.5.0 is an old package (released in 2018) and uses
# removed Flask internals. It is NOT compatible with modern Flask 3.x.
# Therefore this first Python 3.14-compatible setup keeps Flask 2.2.5.
#
# Your current main.py uses SQLite locally. PostgreSQL is included for the
# later Render deployment stage.

Bootstrap-Flask==2.3.3
Flask-CKEditor==0.5.1
Flask-Login==0.6.3
Flask-WTF==1.2.1
WTForms==3.2.1
Flask-SQLAlchemy==3.1.1
SQLAlchemy==2.0.52
Flask==3.1.3
Werkzeug==3.1.3
gunicorn==26.2.0
bleach==6.3.0

# PostgreSQL driver for the later PostgreSQL/Render stage
psycopg2-binary==2.9.12

# Render start command:
# gunicorn main:app